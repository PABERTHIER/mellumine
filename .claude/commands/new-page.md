---
description: Create a new content page end-to-end (i18n translations, Vue page component with full SEO).
---

Use the `/seo`, `/i18n`, and `/nuxt-vue-patterns` skills to create the page for: $ARGUMENTS

If no arguments were given, ask the user for:

1. Page name / route (e.g., `events`, `faq`, `stream`)
2. Page title (FR + EN)
3. Page subtitle (FR + EN)
4. Meta description (FR + EN — ≤160 chars each, must mention "Mellumine")
5. SEO keywords to use from `miscellaneous.*`
6. OG image path (defaults to `og-image.webp`)
